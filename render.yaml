services:
  - type: web
    name: conan-online-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start --workspace server
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: CLIENT_ORIGIN
        sync: false
